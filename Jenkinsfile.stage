pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "Building branch ${env.BRANCH_NAME}"
                // Add your build steps here, e.g., sh 'make', bat 'build.bat'
            }
        }
    }
}
